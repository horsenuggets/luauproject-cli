local github = require("@self/github")
local prompts = require("@self/prompts")
local shell = require("@self/shell")
local strings = require("@self/strings")
local wally = require("@self/wally")

return {
    github = github,
    prompts = prompts,
    shell = shell,
    strings = strings,
    wally = wally,
}
